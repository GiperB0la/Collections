import Util.array_list;
import Util.linked_list;
import Util.hash_set;
import Util.linked_hash_set;
import Util.tree_set;
import Util.hash_map;
import Util.linked_hash_map;
import Util.tree_map;

public class Main {
    public static void main(String[] args) {
        array_list<Integer> arrl = new array_list<Integer>();
        linked_list<Integer> linl = new linked_list<Integer>();
        hash_set<Integer> hashs = new hash_set<Integer>();
        linked_hash_set<Integer> lhashs = new linked_hash_set<Integer>();
        tree_set<Integer> trees = new tree_set<Integer>();
        hash_map<Integer, String> hashm = new hash_map<Integer, String>();
        linked_hash_map<Integer, String> lhashm = new linked_hash_map<Integer, String>();
        tree_map<Integer, String> treem = new tree_map<Integer, String>();
    }
}